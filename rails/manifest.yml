---
applications:
  .:
    name: mewars-api
    framework:
      name: rails3
      info:
        mem: 256M
        description: Rails Application
        exec: 
    infra: aws
    url: ${name}.${target-base}
    mem: 128M
    instances: 1
    services:
      mewars-api-mysql:
        type: mysql
      mewars-rabbitmq:
        type: rabbitmq
